
users:
	id
	username
	password
	email

login_token:
	id
	selector
	valhash
	userid
	expires

perms:
	id
	name

user_perm_rels:
	id
	userid->users
	permid->perms

books:
	id
	title
	author->user

book_colabs:
	id
	bookid->book
	userid->user

book_whitelist:
	id
	color (white/black)
	bookid->book
	userid->user

pages:
	id
	title
	text
	author->user

page_colabs:
	id
	userid->users(id)
	pageid->pages(id)

page_whitelist:
	id
	color (white/black)
	bookid->book
	userid->user

sub_pages:
	id
	book->books(id) [NULL if parent is page]
	parent->pages(id)
	child->pages(id)

subjects:
	id
	text
	description

page_subjects:
	id
	subjectid->subject(id)
	pageid->page(id)

----
Permission Levels:
root
admin
	- edit users
	- edit other pages
	- view locked pages
user
	- edit own/open pages
	- add comments
	- edit theme
	- lock page (to degrees)
	- open page (to degrees)
guest
	- view unlocked pages
